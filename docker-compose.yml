services:
  asset-service-db:
    image: postgres:15
    container_name: asset-service-db
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "5432:5432"
    volumes:
      - asset-service-data:/var/lib/postgresql/data
    networks:
      - asset-service-network

volumes:
  asset-service-data:

networks:
  asset-service-network:
    driver: bridge